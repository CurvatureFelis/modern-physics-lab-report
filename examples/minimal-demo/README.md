# Minimal software test fixture

This directory tests the image-transcription input schema. The values are synthetic software fixtures, not experimental measurements and not a model for expected physical results.

Copy `manual_transcription.csv` to a generated project's `data/processed/` directory, run `analysis/analysis.py`, and confirm that the file is indexed without schema errors.
