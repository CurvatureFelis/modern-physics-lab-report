---
name: modern-physics-lab-report
description: Analyze undergraduate modern-physics laboratory principles, procedures, instrument information, and raw measurements; choose experiment-specific processing, fitting, uncertainty analysis, and plots; then generate a traceable, reproducible LaTeX lab report. Use for 近代物理实验报告、实验数据处理、拟合、误差与不确定度分析. Do not use for inventing missing measurements or forcing agreement with theory.
---

# 近代物理实验报告

从用户提供的实验资料和原始数据建立物理模型，生成可复现的数据分析工程与 LaTeX 报告。根据具体实验选择方法；不要套用固定实验类型或固定拟合模型。

## 不可违反的约束

- 把用户提供的原始数据视为实验结果的唯一依据。不得修改、伪造、补齐、平滑或为接近理论值而筛选数据。
- 原始文件只读保留；复制到 `data/raw/` 后计算。所有派生数据写入 `data/processed/`，不得覆盖原始文件。
- 发现异常值时保留并标记，给出物理或统计依据。若讨论排除，必须同时报告包含与排除异常值的结果，并明确排除对结论的影响。
- 不得凭空补充仪器、精度、允差、实验步骤、环境条件、标准值或教师要求。把用户交付的材料视为其能够取得的完整实验记录：无法由现有材料确定的定量项直接省略，不在正式报告中罗列未上传、无法获取或理想情况下还可增加的资料。只有某项歧义会阻止识别实验对象、单位或核心结果时，才在交互阶段提出一个必要问题。
- 理论值不能冒充实验值。不得为了得到“正确答案”事后更换模型、变量范围、权重或筛选规则。
- 报告中的关键数值、表格和图像应由分析脚本从原始数据生成；禁止手工转录代码已经计算出的结果。
- 保留分析决策、单位换算、数据清洗、拟合与不确定度传播的可追溯记录。
- 实验报告中的数学与物理符号必须遵守 [内置 LaTeX 符号规范](references/latex-symbol-conventions.md)。写入或修改公式前完整读取该文件，并按符号的物理角色处理字体、变体和语义；不得只按字形做机械替换。若当前环境还安装了 `$latex-symbol-conventions`，同时读取其当前版本化权威参考，以完整规范补充内置规范；冲突时用户明确给出的项目级约定优先，其次采用外部完整规范，最后采用本 Skill 的内置规范，并在全文保持一致。

## 每次任务的工作流

### 1. 盘点材料并建立输入清单

递归检查用户指定的实验目录，识别 PDF、图片、Word、Markdown、LaTeX、Excel、CSV 与文本数据。不要仅凭文件名判断内容。先输出：

```text
已获得材料：
✓/✗ 实验原理
✓/✗ 实验步骤与仪器
✓/✗ 原始数据

材料使用范围：
- 以上材料视为本次分析的完整可用记录
```

读取 PDF、文档、电子表格或图片时，使用当前环境中相应的文档、PDF、电子表格或图像能力；遵守这些能力要求的完整读取和视觉检查流程。图片表格、手写记录和软件界面中的数值统一转录到 `data/processed/manual_transcription.csv`，逐项核对，并把转录文件标为派生数据而非原始数据。开始转录前阅读 [图片数据转录规范](references/transcription-schema.md)。

### 2. 建模后再计算

提炼实验目的、研究对象、直接/间接测量量、自变量、因变量、控制量、目标量、单位和理论关系。输出简短的“实验建模”摘要，包括最终目标、测量量、理论关系和建议处理方法。

然后读取 [物理建模与数据分析](references/analysis-guidance.md)，据具体模型决定平均、统计量、变量变换、拟合、残差诊断、误差传播和标准值比较。不要默认所有实验都做线性拟合、R² 或完整不确定度预算。

### 3. 审计数据

检查工作表/数据表含义、行列对应、单位、数量、重复测量、缺失值、重复值、范围、实验条件映射和可能的录入错误。单位或列含义含糊时，不进行会改变结论的猜测；若不影响核心结果则省略相应派生量，若阻断核心分析才向用户核对。

任何异常判断都先生成仅含标记和理由的审计表，不改变原值。保留全数据分析；只有具备事先规则或合理物理依据时才生成对照的排除分析。

### 4. 建立可复现工程

若当前实验还没有工程骨架，运行：

```text
python <skill-dir>/scripts/init_lab_project.py <实验输出目录>
```

工程模板位于 `assets/project-template/`。将输入复制到 `data/raw/`，并在目录外维护 `data/raw_manifest.sha256` 或等价清单。先编辑 `config/experiment.yaml` 描述本实验的文件、列、单位、模型与已知不确定度，再按需要改造 `analysis/analysis.py`。模板是骨架，不是固定模型；删除不适用的示例配置。

分析流水线保持：原始数据 → 校验/单位统一 → 派生量 → 统计/拟合 → 不确定度 → 结果文件 → 表格/图像 → 报告。脚本修改原始数据后应能一键重建全部派生产物。

### 5. 生成报告

先完整读取 [内置 LaTeX 符号规范](references/latex-symbol-conventions.md) 并检查项目本地样式文件；当前环境存在 `$latex-symbol-conventions` 时，再按其要求加载完整的版本化权威参考。随后读取 [LaTeX 报告与交付规范](references/report-guidance.md)。以 `assets/project-template/report/report.tex` 为起点，将脚本生成的 `results.tex`、表格和图像插入报告。原理和步骤只基于材料重组，不虚构操作。

实验原理的详略应跟随用户资料的细致程度和课程要求。资料给出完整理论、推导和脉冲/测量机制时，不要为了简短而只保留结论公式；应清楚说明研究对象、物理图像、关键方程的来由、适用条件、各步骤为何能测到目标量，以及理论模型怎样连接后续拟合。可以压缩与本实验数据处理无关的历史或旁支内容，但不得省略理解实验装置、测量序列、拟合模型、误差来源所需的理论链路。严禁机械复制讲义，应重组为连贯且足以独立阅读的实验原理。

实验原理或报告论证需要示意图时，先判断图像角色和精度。用户资料中的实验图片若数据、坐标、拟合曲线和关键标注清晰可辨，应直接裁切并插入最终 PDF，保留原始视觉证据；不得为了形式统一而无必要重绘。图片缩放后若数值或线条不清，才按可辨内容使用 TikZ、PGFPlots、circuitikz 或 Python 重绘，不得补造不可辨信息。裁切不得去掉会改变含义的坐标、图例或说明。实验数据可视化若有机器可读原始数据，仍应由脚本生成；仅有清晰实验截图时可直接使用截图。图注只写图中对象、变量、条件和必要的物理含义，不写“根据……重绘”“非实验波形”“非实验数据”“来源于……”等制作说明；图像真实性与重绘范围记录在项目内部审计材料中，不进入正式报告。

正式报告的“数据处理”只写读者复核实验所需的物理量定义、计算公式、变量变换、拟合模型、数据范围、参数与必要诊断，不写 AI、自动化代理、脚本如何工作、文件哈希、目录组织、转录工作流或“结果如何回填报告”等制作过程。可复现工程细节保留在代码、配置或项目记录中，不进入正式正文。

正式报告使用实验论文式客观表述，直接陈述物理原理、实验条件、处理方法、结果与限制。不要出现“根据讲义/资料/图片/用户提供内容”“依据某材料”“某内容不能作为正式报告/不能作为某依据”“AI 如何处理”等来源提示、制作过程或对话过程话术。参考文献条目可正常列出明确题名，但正文与图注不以资料来源作为句子开头或论述框架。

“不确定度与误差分析”直接写结论：给出已经计算出的不确定度或拟合分散，指出数据实际呈现的偏离、主导误差机制、影响方向以及对结果的后果。把用户现有材料视为全部可获得记录，不写“尚缺原始数据”“未提供协方差”“有待补充重复测量”“需要更多数据后才能……”等上传完整性说明，也不列理想化的数据愿望清单。现有材料不支持某项数值不确定度时，省略该数值，只做能够由实验现象和仪器机制支持的定性或半定量结论；不得为满足“直接写结论”而编造误差数值、仪器精度或残差。异常数据仍须如实报告和讨论。

“资料来源”只列可明确命名的教材、实验讲义、课程课件、论文或标准，并使用实际标题。不要写“用户提供的资料/图片/论文”“附件”“聊天中提供”等来源描述。实验者拍摄的原始数据照片属于实验记录，应在原始数据或图注中说明为“实验记录”或“软件拟合界面”，通常不列入参考资料。

默认章节为实验目的、实验原理、实验仪器、实验内容与步骤、原始数据、数据处理、实验结果、不确定度与误差分析、思考与讨论、实验结论；按实验逻辑合理调整，不机械保留空章节。

### 6. 验证并交付

- 重跑分析脚本，确认无人工结果硬编码、无写入 `data/raw/`、结果可由输入重建。
- 抽查至少一条“原始值 → 中间量 → 最终量”链路；核对表格、正文和图中的数值一致。
- 对拟合检查参数不确定度、适用区间和残差；对排除分析检查双轨结果都存在。
- 使用 XeLaTeX/latexmk 编译报告并检查日志；渲染 PDF 逐页视觉检查，修复溢出、乱码、表格或图像问题。
- 按内置符号规范的审计清单检查符号漂移；若已加载 `$latex-symbol-conventions`，同时执行其完整审计。尤其检查向量/矩阵、量子算符、正体常数、微分、单位、描述性下标与希腊字母变体；编译成功不能代替语义审计。
- 完成最终视觉检查并确认最终 PDF 可读后，先运行 `python <skill-dir>/scripts/cleanup_lab_project.py <实验输出目录>` 扫描临时文件，再以 `--apply` 自动删除扫描出的允许项。只清理可再生成的 `tmp/`、`__pycache__/`、`.pytest_cache/`、LaTeX 辅助文件和非最终渲染图；必须保留原始资料、`data/raw/`、处理数据、分析结果、脚本、配置、图表、LaTeX 源文件和最终 PDF。输出删除清单；遇到允许列表之外的可疑文件时保留并报告，不扩大删除范围。
- 最终列出输入、分析方法、异常值处理、关键结果、复现命令和产物路径；不要向用户重复索要其无法取得的实验数据。

如果当前环境缺少必要解析库或 LaTeX 工具，明确说明未完成哪项验证及其影响；不要声称已通过。
